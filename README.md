# CPU-Scheduler
Advanced CPU Scheduling Engine which models the behavior of a single-core CPU handling multiple processes with varying arrival times, burst times, and priorities. It accounts for real-world system overhead by simulating Context Switching delays.
